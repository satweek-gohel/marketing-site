/** @type {import('next').NextConfig} */
const nextConfig = { 
    // Enable static export
        output: 'export',
        images: {
            unoptimized: true,
          },
      
};

export default nextConfig;
